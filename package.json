{
  "name": "createos-sandbox-ghar",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "oxlint src test",
    "typecheck": "tsc --noEmit",
    "build:template": "bun run template/build.ts"
  },
  "dependencies": {
    "@nodeops-createos/sandbox": "link:@nodeops-createos/sandbox"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.8.71",
    "@cloudflare/workers-types": "5.20260703.1",
    "oxlint": "1.72.0",
    "typescript": "6.0.3",
    "vitest": "3.2.4",
    "wrangler": "4.107.0"
  }
}
